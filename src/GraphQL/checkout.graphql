mutation checkoutLink($transactionId: ID!, $embed: Boolean, $paymentMethods: [DepositMethod]) {
    link: checkoutLink(transactionId: $transactionId, embed: $embed, paymentMethods: $paymentMethods)
}
